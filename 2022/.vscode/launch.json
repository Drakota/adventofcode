{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug current C file",
            "program": "${workspaceFolder}/build/${relativeFileDirname}/solution",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "[2022] Compile current C file",
        }
    ]
}